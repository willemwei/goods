export const user = (state) => {
  return state.user;
};

export const cartList = (state) => {
  return state.cartList;
};

export const orderProcess = (state) => {
  return state.orderProcess;
};

export const addressList = (state) => {
  return state.addressList;
};

export const alert = (state) => {
  return state.alert;
};
